---
 - hosts: appservers
   gather_facts: yes
   tasks:
    - name: create a file
      file:
        path: /home/ansadm/playbook/testserver
        state: touch
    - name: copy the file
      copy:
        dest: /home/ansadm/playbook/testserver
        src: /home/ansadm/testserver
        remote_src: yes
