---
 - hosts: appservers
   gather_facts: yes
   tasks:
    - name: create a file
      shell: touch test1
    - name: copy the file
      copy:
        dest: /home/ansadm/playbook/test1
        src: /home/ansadm/test
        remote_src: yes
