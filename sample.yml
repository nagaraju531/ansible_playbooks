---
 - hosts: appservers
   gather_facts: yes
   tasks:
    - name: create a file
      file:
        path: /home/ansadm/playbook/testserver1
        state: touch
    - name: copy the file
      copy:
        src: /home/ansadm/playbook/testserver1
        dest: /home/ansadm/
        remote_src: yes
